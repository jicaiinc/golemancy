# Copy this to .env.e2e.local and fill in your API keys
# .env.e2e.local is gitignored

# At least one API key is needed for AI tier tests
TEST_GOOGLE_API_KEY=
TEST_OPENAI_API_KEY=
TEST_ANTHROPIC_API_KEY=

# Active provider for tests (google | openai | anthropic)
TEST_ACTIVE_PROVIDER=google
