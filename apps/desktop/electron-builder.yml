appId: ai.golemancy
productName: Golemancy
copyright: Copyright © 2026 Jicai, Inc.

publish:
  provider: github
  owner: jicaiinc
  repo: golemancy
  releaseType: draft

directories:
  output: release
  buildResources: resources/build

forceCodeSigning: true
afterPack: ../../scripts/electron-builder-after-pack.mjs

files:
  - out/**/*
  - "!out/**/*.map"
  - package.json

extraResources:
  - from: resources/runtime
    to: runtime
    filter: ["**/*"]
  - from: resources/server
    to: server

asar: true
asarUnpack: []  # native modules are in extraResources, not in ASAR

mac:
  category: public.app-category.developer-tools
  icon: resources/build/icons/mac/icon.icns
  identity: "Jicai, Inc. (NDX2ZZ8U2R)"
  target: [dmg, zip]
  hardenedRuntime: true
  entitlements: resources/build/entitlements.mac.plist
  entitlementsInherit: resources/build/entitlements.mac.plist
  notarize: true
  extendInfo:
    NSMicrophoneUsageDescription: "Golemancy needs microphone access for speech-to-text input."

dmg:
  artifactName: ${productName}-${version}-mac-${arch}.${ext}

linux:
  target: [deb, rpm]
  category: Development
  icon: resources/build/icons/png
  artifactName: ${productName}-${version}-linux-${arch}.${ext}

win:
  target: [nsis, msi]
  icon: resources/build/icons/win/icon.ico
  artifactName: ${productName}-${version}-win-${arch}.${ext}
  azureSignOptions:
    publisherName: "Jicai, Inc."
    endpoint: https://eus.codesigning.azure.net/
    codeSigningAccountName: golemancy
    certificateProfileName: golemancy
